<%if(success.length){%>
<!-- //console.dir(success) shows us that success is present and empty, that why use length to check for presence -->
<div class="alert alert-success alert-dismissible fade show" role="alert">
  <%= success %>
  <button
    type="button"
    class="btn-close"
    data-bs-dismiss="alert"
    aria-label="Close"
  ></button>
</div>

<% } %>
